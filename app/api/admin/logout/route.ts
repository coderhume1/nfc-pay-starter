import { NextRequest, NextResponse } from 'next/server'; export async function POST(req:NextRequest){const r=NextResponse.redirect(new URL('/admin',req.url));r.cookies.set('admin_auth','',{path:'/',maxAge:0});return r}
