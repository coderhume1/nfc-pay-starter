{
  "name": "nfc-pay-netlify",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "( if [ -n \"$DATABASE_URL\" ] && [[ \"$DATABASE_URL\" != *\"ep-your-project-pooler\"* ]] && [[ \"$DATABASE_URL\" != *\"USER:PASSWORD\"* ]]; then npx prisma migrate deploy || npx prisma db push; else echo 'DATABASE_URL not set or placeholder, skipping Prisma'; fi ) && next build",
    "start": "next start",
    "postinstall": "prisma generate"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^5.13.0",
    "prisma": "^5.22.0",
    "typescript": "^5.4.0"
  }
}